<h1>Eat-Dis-Burger!</h1>

<main>
  <div class="container">
    <div class="row">
      <div class="col">
        {{#each burgers}}
          {{#unless this.devoured}}
            <div>
              <form action="/burgers/{{this.id}}?_method=PUT" method="POST">
                <button type="submit"> {{this.burger_name}}</button>
              </form>
            </div>
          {{/unless}}
        {{/each}}
      </div>

      <div class="col">
        {{#each burgers}}
          {{#if this.devoured}}
            <div>
              <div>
                {{this.burger_name}}
              </div>
            </div>
          {{/if}}
        {{/each}}
      </div>
    </div>
  </div>
</main>

<h2>Add A Burger!</h2>

<div class="row">
  <div class="col">
    <form action="/burgers" method="POST">
      <input type="text" id="name" name="burger_name" placeholder="Request Burger">
      <button type="submit">Add The Burger</button>
    </form>
  </div>
</div>